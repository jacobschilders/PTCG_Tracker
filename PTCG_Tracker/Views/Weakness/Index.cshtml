@model IEnumerable<PTCG_Tracker.Models.Weakness.WeaknessListItem>

@{
    ViewBag.Title = "Index";
}

<br />
<br />
<br />
<br />
<br />
<br />
<div class="card">
    <div class="card-header">
        Weakness/Index
    </div>
    <div class="card-body">
        <h4 class="card-title">Weakness List</h4>
        <p class="card-text">View all the "Weaknesses" currently in the database.</p>
    </div>
</div>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WeaknessId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Value)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.WeaknessId)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Type)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Value)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.WeaknessId }) |
        @Html.ActionLink("Details", "Details", new { id = item.WeaknessId }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.WeaknessId })
    </td>
</tr>
    }

</table>
